#!/usr/bin/python
# -*- coding: utf-8 -*-

def print_poetry_cmd():
    poetry_cmd = """
京中有善口技者，从此君王不早朝。
巧妇难为无米之炊，有米自有巧妇来吹
在夹缝中求生存，必须得坚持不懈的、坚持到底自身有过硬的本事
情关难越，谁悲单身之狗。
关山难越，谁悲失足之女。
萍水相逢，尽是他人之妻。
老当益壮，不移好色之心。
穷且益坚，不坠孟德之志。
囊中羞涩，不改风流之志。
老骥伏枥，志在佳人；穷途末路，心系红颜。
廉颇虽老，尚能干饭；孟德虽虚，仍念二乔。
生老病死三千疾，唯有相思不可医

山海关上挂驴鞭--天下第一der
老母猪带胸罩--一套又一套

口活堪比狗喝水，指法灵活似魔鬼。

旧时王谢堂前燕，飞入寻常百姓家。
忽有故人心上过，回首山河已是秋。
他朝若是同林雪，此生也算共白头。
鸳鸯被里成双夜，一树梨花压海棠。
情不知所起，一往而深
恨不知所终，一笑而泯
天长地久有时尽，此恨绵绵无绝期。
天涯地角有穷时，只有相思无尽处。
山无陵，江水为竭...乃敢与卿绝！
十年生死两茫茫，不思量，自难忘。
还君明珠双泪垂，恨不相逢未嫁时。
两情若是久长时，又岂在朝朝暮暮。
赌书消得泼茶香，当时只道是寻常。
此情可待成追忆，只是当时已惘然。
人生若只如初见，何事秋风悲画扇。
衣带渐宽终不悔，为伊消得人憔悴。
相见时难别亦难，东风无力百花残。
执手相看泪眼，竟无语凝噎。
一别两宽，各生欢喜。
人面不知何处去，桃花依旧笑春风。
花谢花飞花满天，红消香断有谁怜？

惊鸿一瞥，宛若游龙。
只应天上有，人间能得几回闻？
回眸一笑百媚生，六宫粉黛无颜色。
昔日芙蓉花，今成断肠草。
相看两不厌，只有敬亭山。
陌上人如玉，娘子世无双。

北方有佳人，绝世而独立，一顾倾人城，再顾倾人国。
云想衣裳花想容，春风拂槛露华浓。
娴静犹如花照水，行动好比风扶柳。

钟山风雨起苍黄，百万雄师过大江。
虎踞龙盘今胜昔，天翻地覆慨而慷。
可上九天揽月，可下五洋捉鳖，谈笑凯歌还。
为有牺牲多壮志，敢教日月换新天。
怅寥廓，问苍茫大地，谁主沉浮？
关关关难过关关过，事事难成事事成

上流社会均是下流之人
仗义每从屠狗辈，负心多是读书人

天下英雄如过江之鲫，浩浩荡荡，而我独是垂钓之人

终日奔波只为饥，方才一饱便思衣；
衣食两般皆俱足，又思娇娥美貌妻；
娶得美妻生下子，恨无田地少根基；
良田置的多广阔，出门又嫌少马骑；
槽头扣了骡和马，恐无官职被人欺；
七品县官还嫌小，又想朝中挂紫衣；
一品当朝为宰相，还想山河夺帝基；
心满意足为天子，又想长生不老期；
一旦求得长生药，再跟上帝论高低。
不足不足不知足，人生人生奈若何？
若要世人心满足，除非南柯一梦兮。

饥不择食，慌不择路，寒不择衣，贫不择妻

八千里路云和月 三十功名尘与土
报君黄金台上意，提携玉龙为君死
怒从心头起、恶向胆边生
天子一怒，伏尸百万；匹夫一怒，血溅三尺,老子一怒,就怒了一下
月黑风高夜，杀人放火天


当知足凌驾于自卑之上，幸福将会贯穿整个人生。人除了生病以外，所感受到的痛苦都是价值观带来的。人只有在知足的时候最富有，也只有在珍惜的时候最幸福。不要和别人刻意展示的精彩片段做比较。没有什么东西是永恒的。幸福既不属于富人，也不属于穷人。幸福只属于知足的人。世人皆因攀比之心而受困。殊不知攀比之心是制造痛苦的牢笼。唯有知足之人方能破茧重生。真正正的贫穷，不是囊中空空，而是欲壑难填。

不同年纪总把眼前难题当 “大事”：小学为背书哭，中学为考试愁，大学为作业烦，工作为失误慌。回头再看，那些曾让我们辗转难眠的 “天大的事”，不过是当时放大的小风浪，早成了不值一提的过往。

恶：贪婪、虚荣、自私、嫉妒、懒惰、傲慢、恐惧、偏见、怨恨
善：共情、善良、勇气、诚信、感恩、责任感、宽容、乐观

买了十斤牛肉，八斤牛逼

天涯地角有穷时，只有相思无尽处
晓看天色暮看云，行也思君，坐也思君
人有生老三千疾，唯有相思不可医
城南小陌又逢春，只见梅花不见人

一念起，天涯尽在咫尺，一年灭，咫尺即是天涯

无论你遇见谁，他都是你生命该出现的人，绝非偶然，他一定会教会你一些什么

天地不仁，以万物为刍狗,天地不仁，以万物为刍狗
"""
    print(poetry_cmd) 

def print_english_cmd():
    print("english usage command:")
    english_cmd = """
Can you can a can as a canner can can a can?
你能像罐头工人一样把罐头装起来吗

How much wood would a woodchuck chuck if a woodchuck could chuck wood? He would chuck, he would, as much as he could and chuck as much wood as a woodchuck would if a woodchuck could chuck wood

freedom is not free
   """
    print(english_cmd) 

def print_mysql_secure_installation_cmd():
    mysql_secure_installation_cmd = """

############################################################## overview ########################################################################

mysql_secure_installation 是 MySQL 官方提供的交互式安全配置脚本，专门用于快速加固 MySQL 服务器的安全设置，是新手部署 MySQL 后必做的核心操作之一。它通过引导式问答，
帮你修复默认安装中存在的安全漏洞（比如空密码、匿名用户、远程 root 访问等）。

######################################################### 核心作用（解决的问题） ###################################################################

1. 核心作用（解决的问题）
MySQL 默认安装后存在以下安全风险，该脚本可一键修复：
root 账户默认无密码或密码过于简单；
存在匿名用户（任何人可无需账号登录数据库）；
root 账户允许远程主机访问（增加被攻击风险）；
存在默认的 test 数据库（可被任意用户访问，用于测试但生产环境需删除）；
清理权限表的冗余授权（确保权限设置生效）。

######################################################### 非交互式执行（批量部署） #################################################################

yum install mariadb-server -y
systemctl start mariadb

# CentOS/RHEL 7/8 系统
yum install -y expect

expect -c "
spawn mysql_secure_installation
expect \"Enter current password for root:\"
send \"\r\"
expect \"Set root password?\"
send \"Y\r\"
expect \"New password:\"
send \"redhat\r\"
expect \"Re-enter new password:\"
send \"redhat\r\"
expect \"Remove anonymous users?\"
send \"Y\r\"
expect \"Disallow root login remotely?\"
send \"n\r\"
expect \"Remove test database and access to it?\"
send \"Y\r\"
expect \"Reload privilege tables now?\"
send \"Y\r\"
expect eof
"
   """
    print(mysql_secure_installation_cmd) 

def print_journalctl_cmd():
    journalctl_cmd = """

Linux 日志有“syslog 标准作为基础”，但在 systemd 和现代应用下，已经演变为“多格式并存、中心化收集”的体系。

systemd-journald.service 是 Linux 系统中 systemd 日志体系的核心服务，负责收集、存储和管理系统日志。可以把它理解成“系统日志的统一入口和缓冲中心”。
换句话说：所有日志先汇总到 journald 日志(二进制格式),通过命令 journalctl 可以按多维度查询

######################################################### 日志产生的函数和输出 ####################################################################


| 类型       | 函数          | 写入位置     | 最终去向
| ---------- | ------------- | ------------ | ------------------
| 内核       | printk        | ring buffer  | journald
| syslog程序 | syslog()      | /dev/log     | journald → rsyslog
| systemd服务| stdout/stderr | pipe         | journald
| 应用程序   | logging框架   | 文件/stdout  | 不固定
| 手动写文件 | fopen         | 文件         | 文件
| 控制台输出 | write         | /dev/console | 终端


journald 和 rsyslog 的关系:
程序 → journald → rsyslog → 文件

默认情况下如果关闭 systemd-journald 服务，rsyslog 配置文件中指定的 /var/log/secure /var/log/message 等文件都不会产生新的日志,logger 指令测试也无日志
systemctl stop systemd-journald.socket
systemctl stop systemd-journald.service

############################################################## instance ########################################################################

默认分页显示（类似 less），按 ↑/↓ 滚动、空格翻页、q 退出。

journalctl -b          # 仅看本次启动后的日志（boot）
journalctl -b -1       # 查看上一次启动的日志
journalctl -k  # 等价 dmesg，只看内核日志
journalctl -k -b  # 本次启动的内核日志


1. 按服务（Unit）过滤（高频）
journalctl -u nginx.service  # 查看Nginx服务日志（.service可省略）
journalctl -u sshd -f        # 实时跟踪SSH服务日志
journalctl -u docker -u kubelet  # 同时查看多个服务


2. 按日志级别过滤（排查错误）
级别从高到低（0-7）：emerg(0)、alert(1)、crit(2)、err(3)、warning(4)、notice(5)、info(6)、debug(7)。
journalctl -p err          # 仅显示错误及以上级别（err/crit/alert/emerg）
journalctl -p warning      # 警告及以上
journalctl -u nginx -p err --since today  # 今天Nginx的错误日志


3. 实时查看今天SSH服务的警告及以上日志
journalctl -u sshd -p warning --since today -f

4. 查看上一次启动的内核错误日志
journalctl -b -1 -k -p err

5. 导出最近1小时Nginx日志到文件
journalctl -u nginx --since "1 hour ago" --no-pager > nginx_log_$(date +%Y%m%d%H%M).log

6. 查看系统所有错误日志，倒序显示50行
journalctl -p err -r -n 50

############################################################### others #########################################################################

aa --show journald
aa --show rsyslog
aa --show logger
aa --show audit
aa --show dmesg

   """
    print(journalctl_cmd) 

def print_logger_cmd():
    logger_cmd = """

############################################################## overview ########################################################################

在编程和系统运维中，logger 是一个非常实用的Unix/Linux 系统命令，主要用于将自定义日志消息写入系统日志（如 /var/log/syslog 或 /var/log/messages），替代直接手动编
辑日志文件，是标准化管理系统日志的核心工具。

############################################################### option #########################################################################

logger [选项] "要记录的日志消息"


选项	全称	        作用说明												示例
-p	--priority	指定日志优先级（格式：设施。级别），默认是 user.notice		logger -p user.warning "磁盘空间不足"
-t	--tag	    给日志添加标签（标识日志来源），便于后续筛选				logger -t backup_script "备份完成"
-f	--file	    将指定文件的内容作为日志写入（替代手动输入消息）			logger -f /tmp/error.log
-i	--id	    在日志中包含 logger 进程的 PID（进程 ID），便于定位关联进程	logger -i "启动监控进程"
-s	--stderr	同时将日志输出到标准错误（屏幕），而不仅是日志文件			logger -s "关键错误：数据库连接失败"
-n	--server	将日志发送到远程日志服务器（需配合 -P 指定端口）			logger -n 192.168.1.100 -P 514 "远程日志测试"
-P	--port	    指定远程日志服务器的端口（默认 514，对应 syslog 服务）		同上
-u	--socket	将日志写入指定的 Unix 域套接字（替代默认的 /dev/log）		logger -u /dev/log_custom "自定义套接字日志"


优先级由「设施（facility）+ 级别（level）」组成，用小数点分隔：

设施：标识日志产生的来源类别，常用值：
user（用户级消息，默认）、syslog（syslog 自身）、auth（认证相关）、cron（定时任务）、daemon（后台进程）。

级别（从低到高，代表严重程度）：
debug（调试）、info（普通信息）、notice（注意）、warning（警告）、err（错误）、crit（严重错误）、alert（紧急）、emerg（系统级故障）。


############################################################## instance ########################################################################

logger "这是一条测试日志：系统备份开始"


# 写入带标签的日志（不写标签默认是root）
logger -t backup_script -p user.info "备份完成：成功备份 100 个文件"

# tail /var/log/messages
Mar 23 16:14:39 ct7_node02 systemd: Started Session 1 of user root.
Mar 23 16:14:39 ct7_node02 systemd-logind: New session 1 of user root.
Mar 23 16:14:52 ct7_node02 backup_script: 备份完成：成功备份 100 个文件
...


# 既想在屏幕看到提示，又想留存日志：
logger -s -p user.warning "警告：/home 分区使用率已达 85%"

############################################################### others #########################################################################

aa --show journald
aa --show rsyslog
aa --show logger
aa --show journalctl
aa --show audit
aa --show dmesg

# 将日志发送到指定服务器中
aa --show rsyslog_server
# 将日志发送到数据库中
aa --show rsyslog_mysql
# 将日志通过web呈现
aa --show rsyslog_web

   """
    print(logger_cmd) 

def print_logrotate_cmd():
    logrotate_cmd = """

logrotate 是 Linux/Unix 系统自带的日志自动轮转、压缩、清理工具，核心作用是防止日志无限膨胀占满磁盘，同时保留历史日志用于排查与审计。它通过定时任务（cron）自动执行，也支
持手动触发，是服务器日志管理的标配工具，man logrotate 查看详情。

############################################################### 核心作用 ########################################################################

自动轮转：按时间 / 大小切割日志，重命名旧日志（如 access.log → access.log.1）并创建新文件。
自动压缩：默认 gzip 压缩旧日志，大幅节省空间。
自动清理：保留指定数量 / 天数的历史日志，超期自动删除。
脚本扩展：轮转前后执行自定义命令（如重启服务、通知、备份）。
灵活触发：支持按天 / 周 / 月或文件大小触发轮转。

############################################################### 工作原理 #########################################################################

1、读取配置：加载全局 /etc/logrotate.conf 与 /etc/logrotate.d/ 下的应用专属配置。
2、条件判断：检查日志是否存在、是否为空、是否达到时间 / 大小阈值。
3、执行轮转：
  重命名旧日志（log → log.1，旧版本依次后移）。
  压缩（log.1 → log.1.gz）。
  删除超期日志（按 rotate N 保留 N 份）。
4、收尾动作：执行 postrotate 脚本（如 nginx -s reload），确保应用写入新日志。

######################################################## /etc/logrotate.conf ###################################################################

指令			说明					示例
daily/weekly/monthly	轮转周期（天 / 周 / 月）		daily
rotate N		保留 N 份历史日志			rotate 7（保留 7 天）
size SIZE		按大小触发（k/M/G）			size 100M
compress		压缩旧日志（gzip）			compress
delaycompress		延迟压缩（下次轮转再压）		delaycompress
create MODE USER GROUP	新建日志的权限与属主			create 0640 nginx nginx
notifempty		空日志不轮转				notifempty
missingok		日志不存在不报错			missingok
copytruncate		先复制再截断（适配不支持 reopen 的程序）copytruncate
postrotate/endscript	轮转后执行脚本				单独配置app的日志规则使用（例如nginx）

##################################################### /etc/cron.daily/logrotate ################################################################

每天自动触发 logrotate 执行日志轮转，并在执行失败时记录告警日志。

cat /etc/cron.daily/logrotate 
#!/bin/sh

/usr/sbin/logrotate -s /var/lib/logrotate/logrotate.status /etc/logrotate.conf
EXITVALUE=$?
if [ $EXITVALUE != 0 ]; then
    /usr/bin/logger -t logrotate "ALERT exited abnormally with [$EXITVALUE]"
fi
exit 0

/usr/sbin/logrotate：logrotate 的主程序路径（系统默认位置）；
-s /var/lib/logrotate/logrotate.status：
  -s（--state）：指定状态文件路径，这个文件是 logrotate 的「记忆账本」；
  作用：记录每个日志文件最后一次轮转的时间 / 次数，避免同一天内重复轮转（比如全局配置是 weekly，即使脚本每天执行，logrotate 也会通过这个文件判断是否满足轮转条件）；

############################################################## advanced ########################################################################

# create new (empty) log files after rotating old ones
create

# use date as a suffix of the rotated file
dateext                            # 全局默认：轮转后的日志用日期作为后缀（如 access.log-20260323，而非默认的 access.log.1）
# uncomment this if you want your log files compressed
#compress                         # 全局默认：不压缩（注释掉了，如需压缩可取消注释，压缩格式为 gzip）

dateext 是很实用的配置：相比默认的数字后缀（.1/.2），日期后缀（-20260323）更直观，能一眼看出日志对应的时间。
create 确保轮转后应用有新的日志文件可写，不会因旧文件被重命名导致日志写入失败。

wtmp 日志专属规则（系统登录日志）
/var/log/wtmp {  # 针对 /var/log/wtmp 文件（记录用户登录/注销信息）的专属规则
    monthly      # 覆盖全局的 weekly：每月轮转一次
    create 0664 root utmp  # 轮转后新建 wtmp 文件，权限 0664，属主 root，属组 utmp
	minsize 1M   # 即使到了每月轮转时间，若文件小于1M也不轮转（避免空文件/小文件频繁轮转）
    rotate 1     # 覆盖全局的 rotate 4：只保留1份历史日志（即只保留上个月的 wtmp）
}
wtmp 特点：这个日志文件是二进制格式（不是纯文本），记录系统登录记录，无需保留过多历史，所以只保留 1 份（对应指令last）。


btmp 日志专属规则（失败登录日志）
/var/log/btmp {  # 针对 /var/log/btmp 文件（记录失败的登录尝试，如密码错误）的专属规则
    missingok    # 即使 btmp 文件不存在，也不报错（避免空文件导致 logrotate 执行失败）
    monthly      # 每月轮转一次
    create 0600 root utmp  # 新建文件权限 0600（仅 root 可读可写，保障安全），属主 root，属组 utmp
    rotate 1     # 只保留1份历史日志
}
btmp 特点：记录敏感的登录失败信息，所以权限设为 0600，仅 root 可访问，防止泄露（对应指令lastb）。


系统专属日志的规则，一般推荐，建议放在 /etc/logrotate.d/ 里

############################################################## instance ########################################################################

一般在安装应用程序的时候，都会自动在 /etc/logrotate.d/ 下安装与之对应的配置文件

cat /etc/logrotate.d/nginx.conf
/var/log/nginx/*.log {
    daily
    missingok
    rotate 7
    compress
    delaycompress
    notifempty
    create 0640 nginx nginx
    sharedscripts
    postrotate
        if [ -f /run/nginx.pid ]; then
            kill -USR1 `cat /run/nginx.pid`
        fi
    endscript
}

# sharedscripts：多日志文件只执行一次脚本。
# kill -USR1：让 Nginx 重新打开日志文件（比 reload 更轻量）。
   """
    print(logrotate_cmd) 

def print_auditd_cmd():
    auditd_cmd = """

auditd 是 Linux 系统的内核级审计守护进程，是 Linux Audit 审计系统的用户空间核心组件，负责收集、存储内核审计子系统捕获的系统安全事件，生成不
可篡改的审计日志，用于安全监控、合规审计与入侵溯源。

############################################################# auditd是什么 ######################################################################

auditd 就是 Linux 系统的「黑匣子」，专门记录谁在系统上干了什么。

1. 它的核心作用
  记录谁、在什么时间、对什么文件、做了什么操作
  监控：读、写、执行、改权限、删文件、改配置
  追踪：用户登录、sudo 使用、系统调用、提权行为
  日志不能被轻易篡改，适合安全审计、等保、溯源
简单说：普通日志记 “发生了什么”，auditd 记 “到底是谁干的”。
  
2. 它能监控哪些事？（最常用）
  谁改了 /etc/passwd、/etc/sudoers
  谁删了某个重要文件
  谁用了 sudo、su
  谁执行了危险命令
  谁修改了权限、属主
  谁登录、登出、切换用户
只要你配置规则，几乎所有系统行为都能记。

############################################################## 工作原理 #########################################################################

auditd 与内核审计模块（audit.ko）协同工作：
1、内核层：通过系统调用钩子（syscall_enter/exit）捕获文件访问、进程执行、权限变更、登录等事件，生成原始审计记录。
2、用户层（auditd）：接收内核事件，按规则过滤、格式化，写入日志文件（默认 /var/log/audit/audit.log），并支持日志轮转、告警与空间管理。
3、核心价值：只记录、不拦截，提供完整操作轨迹，满足等保、PCI-DSS 等合规要求，是安全事件溯源的关键依据。

############################################################### 核心组件 ########################################################################

组件		作用
auditd		守护进程，负责事件收集、日志写入与服务管理
auditctl	命令行工具，配置 / 加载 / 删除审计规则（临时 / 永久）
ausearch	日志检索工具，按时间、用户、文件、关键字查询
aureport	日志报表工具，生成登录、文件操作、系统调用等汇总报告
audispd		审计事件分发器，将日志转发到日志系统（如 syslog、ELK）

############################################################### 配置文件 ########################################################################

1、主配置文件：/etc/audit/auditd.conf（控制日志存储、轮转、空间策略）
log_file = /var/log/audit/audit.log  # 日志路径
log_format = RAW                     # 原始格式（不可篡改）
max_log_file = 100                   # 单文件最大100MB
max_log_file_action = ROTATE         # 满后轮转
space_left = 500                    # 剩余500MB告警
space_left_action = SYSLOG          # 告警方式

Q： auditd 的日志是为啥不是写在/etc/logrotate.d目录下的 ？
A： auditd 是内核级安全审计服务，日志绝对不能断，不能被外部工具随便碰（防止篡改），所以它内置了完整的日志轮转逻辑，不需要、也不推荐用系统 logrotate。

2、规则文件：/etc/audit/rules.d/audit.rules（持久化审计规则）

############################################################### 服务管理 ########################################################################

# 安装（RHEL/CentOS）
yum install -y audit audit-libs

# 启动/开机自启
systemctl enable --now auditd

# 查看状态
systemctl status auditd

# 重启（配置/规则变更后）
systemctl restart auditd

############################################################## instance ########################################################################

# 查看当前加载的规则
auditctl -l

# 删除所有临时规则
auditctl -D

# 从文件加载规则（临时生效）
auditctl -R /etc/audit/rules.d/audit.rules

# 汇总报告
aureport

# 登录报表
aureport -l

# 生成登录失败报告
aureport -l --failed

# 文件操作报表
aureport -f

# 命令执行报表
aureport -x

# 失败事件报表
aureport --failed

# 实时看审计日志（人性化显示）
tail -f /var/log/audit/audit.log | ausearch -i

############################################################## caution #########################################################################

# auditd 服务启动失败解决办法
# 一般为内核启动参数中设置了 audit=0，直接禁用了审计子系统，导致 audited 服务无法启动。
cat /proc/cmdline
# 找到 GRUB_CMDLINE_LINUX 行，删除其中的 audit=0
vi /boot/grub2/grub.cfg

   """
    print(auditd_cmd) 

